# MewCode Agent Loop Checklist

> 本清单在实现前保持未勾选。验收时必须先运行对应命令或执行 tmux 场景，再将实际命令输出、请求日志、pane 观察、文件校验值和进程状态记录到对应条目；不得以代码阅读或预期结果代替运行证据。

## Agent Loop 与停止条件

- [x] **C1：单次请求可自主完成多步任务。**（验证：让本地假 Provider 依次返回“读文件、搜索内容、改文件、最终文字”，期望用户只提交一次请求，系统自动执行全部工具并在第 4 次模型响应后完成）
- [x] **C2：纯文本回复走一次请求快速路径。**（验证：首次模型响应直接返回完整文字，期望 Provider 请求数为 1、没有工具执行并正常提交用户消息和答复）
- [x] **C3：10 次迭代上限精确生效。**（验证：让连续 10 次响应均请求有效工具，期望第 10 批工具正常执行并提交检查点，不发起第 11 次模型请求，最终停止原因为迭代上限）
- [x] **C4：连续未知工具触发停止。**（验证：连续两次响应均只请求未知工具，期望第一次错误结果回灌并继续，第二次错误结果提交后停止，模型请求数恰为 2）
- [x] **C5：有效工具会重置未知工具计数。**（验证：按“未知、有效、未知、未知”返回调用，期望前三次均继续，最后一次才因连续未知工具停止）
- [x] **C6：Provider 和无效流均安全停止。**（验证：分别制造 Provider 异常、缺少结束事件、重复或冲突事件、残缺工具调用和无效 JSON 参数碎片，期望停止原因明确、不执行不完整工具且不发起后续请求）
- [x] **C7：空响应和结束后额外事件被拒绝。**（验证：分别返回正常结束但无文字/工具、结束事件后的额外增量，期望产生无效流停止，不提交该响应）
- [x] **C8：每条退出路径只产生一次最终停止。**（验证：对正常完成、上限、未知工具、取消、Provider 错误和无效流收集事件，期望每轮恰有一个最终停止事件且其后无进度、工具或新请求事件）

## 流式事件与 Token 用量

- [x] **C9：文本和 thinking 增量实时可见。**（验证：用可控异步流在结束前暂停，期望消费者已经收到此前增量，恢复流后才收到完整响应和停止事件）
- [x] **C10：流结束后同时获得完整响应。**（验证：分片发送文本、多个工具调用及参数，期望实时增量顺序不变，最终收集结果包含完整文字、全部调用、调用标识、参数和结束状态）
- [x] **C11：事件流可脱离 TUI 独立消费。**（验证：在不创建界面组件的测试中运行完整循环，期望可仅凭公开事件观察 thinking、文本、工具调用、工具结果、用量、进度和停止状态）
- [x] **C12：模式和进度事件覆盖完整生命周期。**（验证：运行含多个工具迭代的 Normal 与 Plan 场景，期望能观察模式切换、1-based 迭代序号、模型请求阶段、工具完成数、检查点提交和最终停止原因）
- [x] **C13：已知 Token 用量按请求和循环累计。**（验证：让三次请求分别返回确定输入/输出 Token，期望每次事件值正确，累计输入、输出和总量等于逐次相加）
- [x] **C14：缺失 Token 用量明确传播为未知。**（验证：让一次请求缺失全部或部分用量，期望界面和累计事件显示未知，不显示 0、不估算；其他可确定字段仍保持正确）
- [x] **C15：工具前说明文字不会提前结束循环。**（验证：模型先流式输出说明文字再请求工具，期望文字立即展示并随完整工具响应进入历史，工具继续执行且当次不产生完成状态）

## 多工具调度

- [x] **C16：同批调用按安全性和原顺序分段。**（验证：提交“读 A、读 B、写 C、读 D、搜 E、改 F”，期望形成“并发 A/B、串行 C、并发 D/E、串行 F”四个依次执行的段）
- [x] **C17：相邻读工具确实并发。**（验证：用同步屏障和时间戳运行同一读段的多个调用，期望它们在任一调用完成前均已启动，运行区间发生重叠）
- [x] **C18：副作用工具始终严格串行。**（验证：用时间戳运行写、改、命令和夹杂读调用，期望任意两个副作用工具不重叠，且后段不会早于前段结束启动）
- [x] **C19：结果回灌顺序不受完成时序影响。**（验证：让并发读工具按反向顺序完成，期望结果仍按原调用顺序排列，每个调用标识与结果一一对应）
- [x] **C20：普通工具失败相互隔离。**（验证：同一并发读段中一个工具返回错误，期望其他工具正常完成，每个调用均有独立结构化结果，后续执行段仍按规则运行）
- [x] **C21：调度取消会回收任务并补齐结果。**（验证：在并发读段和串行副作用段分别取消，期望未完成任务被回收，已完成结果保留，当前未完成及后续未启动调用均获得取消结果，无残留任务或进程）
- [x] **C22：每个命令调用仍需独立确认。**（验证：同一批或不同迭代连续请求两个相同命令，期望各展示一次新的确认框，前一次决定不会复用）

## 历史检查点与双 Provider

- [x] **C23：每个完整工具迭代立即形成历史检查点。**（验证：运行两个工具迭代后暂停第三次请求，期望历史已包含用户消息、两次完整助手工具请求及各自全部结果，顺序和调用标识合法）
- [x] **C24：模型流中止会丢弃当前部分响应。**（验证：完成两个检查点后在第三次文本流中取消或制造错误，期望第三次部分文本不进入历史，前两个检查点保持不变）
- [x] **C25：工具阶段取消仍提交完整调用结果。**（验证：在一批工具执行中取消，期望该助手工具请求及所有真实/取消结果共同提交，历史中不存在缺少结果的调用）
- [x] **C26：中途停止后的会话可以继续。**（验证：分别在模型流和工具阶段停止后发送下一条普通消息，期望两种 Provider 均能重放已提交历史并生成新答复）
- [x] **C27：OpenAI 可重放多工具多迭代历史。**（验证：捕获 OpenAI 后续请求，期望助手工具调用与逐个工具结果消息顺序合法，包含已提交检查点且不包含未完成响应）
- [x] **C28：Anthropic 可重放多工具多迭代历史。**（验证：捕获 Anthropic 后续请求，期望工具使用块与合并后的工具结果块顺序合法，包含已提交检查点且不包含未完成响应）
- [x] **C29：两种 Provider 的领域行为一致。**（验证：对同一组循环、取消、未知工具和 Plan 场景复用领域测试，期望除协议载荷与可用用量字段外，请求次数、事件、工具结果、停止原因和最终历史一致）
- [x] **C30：thinking 不进入后续模型历史。**（验证：让两种 Provider 产生 thinking 与普通文字后触发下一次请求，期望 TUI 可见 thinking，但捕获的后续请求历史中不含 thinking 内容）

## Plan Mode 与执行计划

- [x] **C31：非空 `/plan` 立即进入只读规划。**（验证：输入 `/plan <任务>`，期望模式先切换为 Plan，再以任务启动 Agent Loop，不需要第二次提交）
- [x] **C32：Plan Mode 只暴露三个读类工具。**（验证：捕获 Plan 中每次 Provider 请求的工具定义，期望恰为读文件、找文件和搜代码，不包含写文件、改文件或执行命令）
- [x] **C33：Plan Mode 在后续讨论中保持只读。**（验证：计划答复结束后输入普通补充要求，包括要求直接修改文件，期望模式仍为 Plan、Provider 仍只看到三个读工具且磁盘无副作用）
- [x] **C34：成功计划结束后保持 Plan 且计划可执行。**（验证：规划循环以完整文字结束，期望状态显示规划完成但仍处于 Plan，最近成功计划被标记为可供执行）
- [x] **C35：`/do` 切回全工具并执行最近计划。**（验证：成功规划后输入 `/do`，期望模式切换为 Normal、Provider 看到全部六个工具，并根据当前任务、调查结果和计划自主执行）
- [x] **C36：没有成功计划时 `/do` 不请求模型。**（验证：新会话以及规划失败/取消后输入 `/do`，期望显示本地“无可执行计划”提示，Provider 请求数不增加）
- [x] **C37：空 `/plan` 被本地拒绝。**（验证：输入仅含 `/plan` 或空白任务，期望模式和历史不变、显示明确提示且不请求 Provider）
- [x] **C38：新规划会清除旧计划就绪状态。**（验证：先完成一个计划，再启动新的 `/plan` 并令其取消或失败，随后输入 `/do`，期望旧计划不会被执行）

## TUI、命令确认与取消

- [x] **C39：多迭代 transcript 顺序真实。**（验证：使用界面测试重放“用户、工具前说明、工具调用/结果、调整说明、最终答复”，期望记录按事件时序出现，纯工具迭代不生成空助手记录）
- [x] **C40：底部状态显示模式、进度和 Token。**（验证：重放模式、迭代、工具进度和用量事件，期望显示 Normal/Plan、当前 1-10 轮、工具完成数及已知或未知 Token，动态内容不造成布局跳动或覆盖）
- [x] **C41：所有停止原因都有稳定可见状态。**（验证：分别重放完成、上限、未知工具、取消、Provider 错误、无效流、无计划和无效命令，期望 TUI 显示对应中文状态且应用保持可用）
- [x] **C42：持续运行期间界面仍可交互。**（验证：模型生成和工具执行期间滚动历史、编辑多行草稿并尝试提交，期望滚轮与编辑及时响应、Alt+Enter 可换行、普通 Enter 不启动并发请求且草稿不丢）
- [x] **C43：模型生成期间可显式取消。**（验证：在长模型流中按 Ctrl+C，期望当前流停止、无新迭代启动、界面显示取消且随后可再次提交）
- [x] **C44：工具执行期间可显式取消。**（验证：分别在并发读组和串行文件工具期间按 Ctrl+C，期望 pending 工具记录全部变为真实结果或取消状态，无 worker error，界面恢复可提交）
- [x] **C45：确认和命令运行期间取消无残留。**（验证：分别在命令 Modal 和含子进程的长命令运行时按 Ctrl+C，期望 Modal 关闭、未批准命令不启动、运行中父子进程均终止、无后台输出继续出现）
- [x] **C46：命令批准与拒绝均可继续循环。**（验证：批准一个带副作用命令并拒绝另一个，期望仅批准命令产生副作用，两次结构化结果均回灌模型并可得到后续答复）
- [x] **C47：退出后的静态记录完整且脱敏。**（验证：完成多迭代、错误、取消和命令确认场景后退出，期望终端保留有界的模式、工具、结果和停止摘要，不含 API Key、traceback 或隐藏完整输出）

## 安全、兼容与自动化

- [x] **C48：循环和并发不扩大文件访问边界。**（验证：在单次与并发工具调用中尝试目录穿越、项目外绝对路径和越界符号链接，期望全部拒绝，外部文件内容不泄露也不改变）
- [x] **C49：既有文件与输出安全保证保持。**（验证：运行原子写入失败、唯一替换零/多匹配、超量查找/搜索和大命令输出场景，期望旧文件不被破坏、输出保持有界且错误结构清晰）
- [x] **C50：配置格式与默认上限兼容。**（验证：使用原有六字段 YAML profile 启动，期望无需新字段；默认循环上限为 10，自动化组装可注入更小上限且不改变 YAML）
- [x] **C51：既有交互和工具行为无回归。**（验证：运行全部既有测试，期望纯文本多轮、thinking、流式显示、Alt+Enter、滚轮、草稿、错误脱敏、双 Provider 和六工具独立行为继续通过）
- [x] **C52：CLI 组装和资源关闭稳定。**（验证：运行 CLI 测试及 `uv run mewcode --help`，期望 Normal/Plan 环境均可组装，正常、异常和取消退出时 Provider 恰好关闭一次）
- [x] **C53：Agent Loop 自动化覆盖完整。**（验证：运行 Agent、Provider、Session、TUI 和工具测试，期望覆盖循环完成、全部停止条件、双路收集、分段并发、错误隔离、历史检查点、Plan Mode、Token 用量和双协议重放）
- [x] **C54：完整质量检查无错误或资源泄漏。**（验证：运行 `uv run pytest -q`、`uv run ruff check .`、`uv run ruff format --check .`、`uv run python -m compileall -q src tests` 和 `git diff --check`，期望状态码均为 0，输出无 pending task、未关闭流、worker error 或残留子进程警告）

## tmux 端到端

- [x] **C55：双 Provider profile 均可启动真实 TUI。**（验证：在 tmux 中分别使用本地 OpenAI 与 Anthropic SSE profile 启动 MewCode，输入自然语言纯文本请求，期望流式回复正常；记录 tmux 版本、pane 尺寸、profile 和服务请求日志）
- [x] **C56：真实三步以上任务无需用户催促。**（验证：在 tmux 中输入一次自然语言任务，要求调查项目、搜索目标并写入或修改文件，期望至少经过三个工具步骤后自动给出最终答复；从 pane、请求日志和磁盘内容交叉核对）
- [x] **C57：tmux 中并发读和副作用串行可观测。**（验证：触发同批多个读工具及夹杂的写、改或命令，通过服务时间戳和 pane 顺序确认读段重叠、副作用段不重叠、结果按调用顺序展示）
- [x] **C58：tmux 中 Plan/Do 完整流程通过。**（验证：输入 `/plan <任务>` 并继续补充要求，确认只出现读工具且磁盘不变；再输入 `/do`，确认切回 Normal、执行计划并产生预期文件变化）
- [x] **C59：tmux 中命令批准和拒绝通过。**（验证：分别批准和拒绝带不同标记文件的命令，期望每次出现独立 Modal，仅批准标记存在，两种结果均驱动模型继续并给出答复）
- [x] **C60：tmux 中各阶段取消可靠。**（验证：分别在模型生成、并发读、串行文件工具、命令确认和命令运行期间取消，期望不再启动后续请求，未完成任务及父子进程消失，草稿和再次提交能力正常）
- [x] **C61：tmux 中安全停止条件可见。**（验证：用确定性 SSE 分别触发连续未知工具、10 次上限和流错误，期望请求数精确、未执行不完整工具、界面显示对应停止原因且随后仍可继续对话）
- [x] **C62：tmux 停止后的历史和静态记录可复查。**（验证：完成两个工具检查点后在第三次流中取消，再发送消息并退出，期望新请求只重放完整检查点；`capture-pane` 保留有界、脱敏的过程与停止摘要）

## 验收证据索引

验收日期：2026-07-25。实现提交：`4a45103 feat: 添加 Agent Loop 与 Plan Mode`。

### 自动化证据

| 覆盖条目 | 实际结果与证据 |
|---|---|
| C1-C8 | `tests/agent/test_runner.py` 实际覆盖纯文本快速路径、多迭代检查点、精确 10 次上限、未知工具重置、Provider/无效流、模型流与工具阶段取消；`tests/agent/test_collector.py` 覆盖空响应以外的损坏事件、结束完整性和流关闭。完整测试通过。 |
| C9-C15 | `tests/agent/test_collector.py` 在流结束前断言增量已到达，并在结束后断言完整文字、调用、顺序和 usage；`tests/agent/test_runner.py` 断言结构化事件、工具前文字、已知/未知 Token 及累计值。 |
| C16-C22 | `tests/agent/test_scheduler.py` 用同步事件与执行轨迹实际断言读任务重叠、副作用不重叠、反向完成仍按原序、失败隔离及取消补齐；`tests/tools/test_execute_command.py` 断言每次命令重新确认和进程组取消。 |
| C23-C30 | Runner 取消测试实际比较提交前后历史；`tests/providers/test_openai.py` 与 `tests/providers/test_anthropic.py` 断言多工具历史的协议载荷顺序、调用 ID、结果合并、thinking 隔离和 usage；Session 测试断言错误后继续。 |
| C31-C38 | `tests/test_session.py` 实际捕获每次请求的工具定义：Plan 恰有 `read_file`、`find_files`、`search_code`，`/do` 恢复六工具；空命令、无计划、新规划失败均不误用旧计划。 |
| C39-C47 | `tests/test_tui.py` 实际运行 Textual pilot，覆盖多迭代顺序、模式/进度/Token、全部停止文案、生成中草稿、Alt+Enter、滚动、显式取消、Modal、再次提交及静态记录脱敏。 |
| C48-C53 | `tests/tools/` 实际执行目录穿越、绝对路径、越界符号链接、原子写失败、唯一替换、截断、命令超时/取消；`tests/test_cli.py` 覆盖原 YAML、环境组装、帮助输出及 Provider 仅关闭一次。 |
| C54 | 最终实际运行 `uv run pytest -q` 得到 `122 passed in 12.62s`；Ruff lint、Ruff format、compileall 和 `git diff --check` 均以状态码 0 完成，未出现 pending task、未关闭流、worker error 或残留子进程警告。 |

### tmux 证据

验收使用本地构建的 tmux 3.5a、真实 Textual TUI、官方 OpenAI/Anthropic SDK 和确定性 SSE 服务。临时项目为 `/tmp/mew-agent-loop-e2e-20260725/project`，服务和 tmux 会话在验收后均已停止。

请求日志 `/tmp/mew-agent-loop-e2e-20260725/requests.jsonl` 实际包含 48 行：OpenAI 36 次、Anthropic 12 次。逐条解析后的关键序列如下：

| 覆盖条目 | 实际结果与证据 |
|---|---|
| C55-C56 | OpenAI 请求 0-3、Anthropic 请求 29-32 均为一次用户输入后 step 0、1、2 三批工具加 step 3 最终答复；两种 profile 每次请求均看到六个工具。`agent-loop.txt` SHA-256 为 `1df8fe3523ea3ea8e28e30c9a9a11e7414fcebba4920766ca49fb07f15e6ac78`。 |
| C57 | OpenAI 请求 41-45 完成“并发读、串行写、并发读、串行改、最终答复”；pane 中工具按调用原序更新。并发读取消实测中 `find_files` 已完成，两个 `search_code` 均补为 `cancelled`；自动化时间屏障同时证明读执行区间重叠、副作用段不重叠。`generated.txt` SHA-256 为 `f2c82decdd7181cf98945929a62598db7e6b477e11f6e0eb0ae97020eff151ad`。 |
| C58 | OpenAI 请求 4-6、Anthropic 请求 34-36 的工具定义均恰为三个只读工具；请求 7-8、37-38 在 `/do` 后恢复六工具并写入 `plan-result.txt`，其 SHA-256 为 `6dc4795b077bcab76cf584d052d4eb82a96d651e7f27bed3b693ce17c60ad444`。 |
| C59 | 两次调用均出现独立确认 Modal。磁盘复核为 `approved-marker=present`、`rejected-marker=absent`，两种结构化结果都触发了后续模型答复。 |
| C60 | 模型流、并发读、命令确认和长命令运行取消后均未启动额外迭代，界面可再次提交。确认取消后 `confirmation-cancelled-marker=absent`；长命令父 PID 31958、子 PID 31959 均已消失，复查无测试服务或 MewCode 残留进程。 |
| C61 | 连续未知工具对应请求 13-14，精确两次；迭代上限对应请求 15-24，step 精确为 0-9；OpenAI 请求 25/46 与 Anthropic 请求 33 均触发无效流停止，不执行残缺工具。长模型取消后 Anthropic 请求 40 成功继续对话。 |
| C62 | pane 与退出后的静态 transcript 实际保留模式、工具、结果、取消和停止摘要，不含 API Key、traceback 或隐藏完整输出；后续请求只包含已经提交的完整工具检查点。 |

### 实测修复记录

tmux 首轮验收发现：模型已经输出部分文本后流损坏时，界面保留了部分文本，却没有把“模型返回了无效的流式响应”写入 transcript。修复 `src/mewcode/tui.py` 后重新执行 OpenAI 和 Anthropic 无效流场景，部分文本与停止原因会同时显示；`tests/test_tui.py::test_invalid_stream_after_partial_text_adds_visible_status` 已固定该回归。

## 功能文档

- [x] **C63：002 双文档与目录可追溯。**（实际：两份 002 文档已记录实现提交 `4a45103`、自动化/tmux 实证和 2026-07-25 官方资料核对日期；Ruby 本地 Markdown 链接检查输出“Markdown 本地链接全部有效”，`git diff --check` 状态码为 0）

## 完成条件

- [x] **C64：Agent Loop 本章验收完成。**（实际：C1-C63 全部有自动化或 tmux 运行证据；首轮 tmux 发现的部分文本无效流状态缺失已修复并重跑，最终无未处理失败项）

## Spec 覆盖自检

| Spec 标准 | Checklist 条目 |
|---|---|
| AC1 | C1 |
| AC2 | C3、C61 |
| AC3 | C4-C5、C61 |
| AC4 | C21、C43-C45、C60 |
| AC5 | C6-C7、C61 |
| AC6 | C9-C11 |
| AC7 | C16-C19、C57 |
| AC8 | C20-C21 |
| AC9 | C22、C45-C46、C59 |
| AC10 | C31-C33、C58 |
| AC11 | C34-C36、C58 |
| AC12 | C12、C39-C41 |
| AC13 | C13-C14、C40 |
| AC14 | C23-C26、C62 |
| AC15 | C15、C39 |
| AC16 | C2 |
| AC17 | C27-C30、C55 |
| AC18 | C42-C45、C60 |
| AC19 | C48-C49 |
| AC20 | C50 |
| AC21 | C47、C51-C54 |
| AC22 | C53-C54 |
| AC23 | C55-C62 |
| AC24 | C63 |

每条 AC 均映射到至少一个可运行或可观察的检查项；实现中的文件、类型或函数重命名不会改变行为验收判定。
