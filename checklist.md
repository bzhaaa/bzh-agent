# MewCode 工具系统 Checklist

> 本清单在实现前保持未勾选。验收时必须先运行对应命令或执行 tmux 场景，再把实际输出、文件校验值、进程状态或 pane 观察结果写入条目证据；不得以代码阅读代替运行证据。

## 实际证据索引

- **E1（完整自动化）：** `uv run pytest -q` 最终实际输出 `97 passed in 12.18s`，结束时无 pending task、worker error、未关闭流或子进程警告。
- **E2（静态检查）：** `uv run ruff check .` 输出 `All checks passed!`；`uv run ruff format --check .` 输出 `36 files already formatted`；`compileall` 与 `git diff --check` 状态码均为 0。
- **E3（tmux 环境）：** 系统原先缺少 tmux，验收时从官方源码在 `/tmp/mewcode-tmux-local` 构建 tmux 3.5a；使用 100x32 pane、`mouse on`、`extended-keys on`，临时项目为 `/tmp/mewcode-e2e-20260725/project`。
- **E4（OpenAI tmux）：** 使用官方 OpenAI SDK连接本地 SSE，在真实 TUI 对话中观察到纯文本、六种文件/搜索操作、路径拒绝、命令 Modal、批准/拒绝、多工具限制、连续调用限制与取消状态。
- **E5（Anthropic tmux）：** 使用官方 Anthropic SDK 连接本地 SSE，在真实 TUI 对话中观察到纯文本、`read_file`、覆盖写、命令 Enter 批准、Esc 拒绝及同批多工具全部拒绝。
- **E6（文件校验）：** `generated.txt` 在成功写入时为 `alpha`、成功编辑时为 `beta`；零匹配和多匹配前后 SHA-256 分别保持 `b6a98d9c...` 与 `f9f3a4ec...`。
- **E7（安全边界）：** tmux 中 `../outside.txt` 与 `outside-link.txt` 均返回项目边界错误；外部文件 SHA-256 始终为 `557c6d0a...`，内容未泄露或修改。
- **E8（命令与取消）：** 批准后 `approved-marker` 存在，拒绝后 `rejected-marker` 不存在；确认框中 Ctrl+C 后 `confirmation-cancelled-marker` 不存在；运行中取消后父 PID 20047、子 PID 20048 均已消失。
- **E9（协议请求）：** 本地服务日志共 52 次请求（OpenAI 39、Anthropic 13）；成功工具回合均为两次请求，多工具仍只追加一次最终请求，连续调用没有第三次请求。
- **E10（静态记录）：** 两个 tmux 会话退出后 `capture-pane` 均保留工具名、complete/rejected/error/cancelled 状态及有界摘要，不含配置 API Key、traceback 或完整隐藏输出。
- **E11（功能总结）：** 新增 `docs/features/001-tool-system.md`，记录实现提交 `33e1c05`、架构、安全边界、配置兼容性、97 项自动化和 tmux 实测证据；`000-current-features.md` 保持原样。

## 工具契约与执行入口

> 实际证据：E1、E2、E4、E5。

- [x] **C1：六个核心工具均可通过统一接口调用。**（验证：运行工具注册与接口测试，期望工具集合恰为 `read_file`、`write_file`、`edit_file`、`execute_command`、`find_files`、`search_code`）
- [x] **C2：工具注册行为稳定。**（验证：运行注册中心测试，期望按名称查询成功、顺序稳定、重复名称及无效定义在注册时被拒绝）
- [x] **C3：两种 Provider 都能接收合法工具定义。**（验证：运行 OpenAI 与 Anthropic 请求载荷测试，期望同一统一定义分别转换为协议要求的 function tool 与 `input_schema`）
- [x] **C4：执行入口统一处理失败。**（验证：分别提交未知工具、无效 JSON、非 object JSON、缺失字段、未知字段、错误类型、超时及主动抛异常工具，期望均得到有界结构化结果且无 traceback）

## 项目路径与文件安全

> 实际证据：E1、E6、E7。

- [x] **C5：项目根目录在启动时固定。**（验证：从两个临时目录分别启动会话并读取同名文件，期望每次只访问对应启动目录）
- [x] **C6：目录穿越和项目外绝对路径被拒绝。**（验证：尝试 `../outside.txt`、多层 `..` 和项目外绝对路径，期望返回 `path_outside_root` 且外部文件内容不泄露、不改变）
- [x] **C7：符号链接不能越过项目边界。**（验证：创建指向项目外文件及目录的链接后执行读、写、改、查找和搜索，期望全部不访问外部目标）
- [x] **C8：UTF-8 原子写入可靠。**（验证：运行新建、覆盖、权限保留、模拟写入失败及模拟替换失败测试，期望成功内容完整，失败时旧字节不变且无临时文件残留）

## 六个核心工具

> 实际证据：E1、E4、E5、E6、E7。

- [x] **C9：读文件支持有界的按行读取。**（验证：读取 UTF-8 文件的指定起始行和行数，期望返回 1-based 行号、实际区间、总行数和正确截断标记）
- [x] **C10：读文件边界错误清晰。**（验证：读取不存在路径、目录、二进制、非 UTF-8、大文件和无效行范围，期望各自返回明确结构化错误且应用继续可用）
- [x] **C11：写文件可新建及覆盖。**（验证：写入嵌套新路径后再覆盖，期望自动创建项目内父目录、磁盘字节正确，并分别报告 `created` 与 `overwritten`）
- [x] **C12：写文件失败不产生部分内容。**（验证：提交目录目标、过大内容、越界链接及模拟提交失败，期望目标不被部分创建或破坏）
- [x] **C13：改文件只接受唯一原文。**（验证：分别构造恰好一次、零次和多次匹配，期望仅一次匹配发生替换，失败结果包含实际匹配数）
- [x] **C14：改文件匹配失败保持原文件不变。**（验证：对零次和多次匹配样例比较操作前后 SHA-256，期望完全一致）
- [x] **C15：文件查找结果安全、排序且有界。**（验证：对普通文件、`.git`、链接目录、非法模式、无匹配和超量结果运行 glob 查找，期望只返回项目内普通文件、相对路径有序并正确标注截断）
- [x] **C16：内容搜索支持字面量、正则和 glob 过滤。**（验证：运行三类查询，期望返回相对路径、1-based 行号和匹配行，顺序稳定）
- [x] **C17：内容搜索边界可观测。**（验证：运行无匹配、非法正则、超量结果、超长行及含二进制/非 UTF-8 文件的场景，期望不崩溃并正确报告空结果、错误或截断）

## 命令执行与确认

> 实际证据：E1、E4、E5、E8。

- [x] **C18：每次命令执行都要求一次新确认。**（验证：连续触发两次相同命令，期望两次均展示完整命令、项目工作目录和超时，前一次批准不被复用）
- [x] **C19：批准后才启动命令。**（验证：批准创建标记文件的命令，期望确认完成前文件不存在，批准后在项目根目录创建且结果含退出码、stdout、stderr 和截断状态）
- [x] **C20：拒绝命令不会启动进程。**（验证：拒绝带标记文件副作用的命令，期望标记文件不存在且模型收到 `user_rejected` 结构化结果）
- [x] **C21：命令退出情形完整且输出有界。**（验证：运行成功、非零退出、启动失败及 stdout/stderr 大输出场景，期望字段完整、双通道被持续排空且截断标记正确）
- [x] **C22：命令超时和取消无残留进程。**（验证：启动含子进程的长命令并分别等待超时、按 `Ctrl+C` 取消，随后检查记录的父/子 PID，期望均已终止且无后台输出继续出现）

## 双 Provider 协议

> 实际证据：E1、E4、E5、E9。

- [x] **C23：OpenAI 流式工具参数可完整组装。**（验证：使用官方 SDK 解析本地 SSE，分段发送调用 ID、名称和 JSON 参数，期望流正常结束后才产生统一工具调用）
- [x] **C24：OpenAI 异常工具流不会执行。**（验证：覆盖残缺 JSON、字段冲突、乱序/重复碎片、同批多调用和异常断流，期望没有工具副作用）
- [x] **C25：Anthropic 流式工具参数可完整组装。**（验证：使用官方 SDK 解析本地 SSE，分段发送 `tool_use` 与 `input_json_delta`，期望 message 正常结束后才产生同一统一工具调用）
- [x] **C26：Anthropic 异常工具流不会执行。**（验证：覆盖残缺 JSON、字段冲突、重复 block、多调用和异常断流，期望没有工具副作用）
- [x] **C27：两种 Provider 可重放完整工具历史。**（验证：对同一领域历史分别捕获两种请求载荷，期望用户消息、全部助手调用、每个对应结果和最终答复顺序合法）
- [x] **C28：两种 Provider 的领域行为一致。**（验证：用同一组假流运行成功、工具错误、多工具和拒绝场景，期望除原生载荷外事件、结果、调用次数和最终历史一致）

## 会话编排与事务性

> 实际证据：E1、E4、E5、E8、E9。

- [x] **C29：纯文本回复仍只请求模型一次。**（验证：运行无工具调用的多轮会话，期望每回合一次 Provider 请求且历史记忆、thinking 展示行为不变）
- [x] **C30：单工具回合恰好执行一次并追加一次最终请求。**（验证：触发一个工具，期望 Provider 请求数为 2、工具执行数为 1、结果以原生格式回灌且最终文字完成后整轮提交）
- [x] **C31：工具失败或拒绝仍可生成最终答复。**（验证：分别制造可恢复工具错误和用户拒绝，期望结构化结果进入第二次请求且 TUI 收到最终文字）
- [x] **C32：同批多个工具全部拒绝。**（验证：首次响应同时请求两个工具，期望执行数为 0、每个调用 ID 都有 `multiple_tools` 结果，并且最多再请求模型一次）
- [x] **C33：最终阶段的第二个工具不执行。**（验证：第二次响应再次请求工具，期望执行数不增加、无第三次 Provider 请求、UI 显示连续调用限制）
- [x] **C34：成功工具回合完整提交历史。**（验证：完成工具回合后发起下一轮，期望 Provider 能看到上轮用户、助手工具请求、工具结果及最终答复）
- [x] **C35：失败或取消不留下半轮历史。**（验证：在首次流、确认等待、工具执行、第二次流、无效流和空最终答复处分别中断，期望历史保持操作前值）
- [x] **C36：已完成文件副作用不因后续取消回滚。**（验证：工具完成写入后取消第二次模型流，期望文件保留、会话历史不提交）

## TUI 与静态记录

> 实际证据：E1、E4、E5、E8、E10；tmux 实测发现并修复工具记录顺序及 Enter 键误拒绝两项问题，修复后重跑通过。

- [x] **C37：工具请求和结果按对话顺序展示。**（验证：使用 Textual Pilot 触发工具回合，期望同一调用记录从 pending 更新为完成或错误，不重复用户消息和助手消息）
- [x] **C38：命令确认 Modal 支持完整键盘操作。**（验证：分别使用 `Y`、`Enter`、`N`、`Esc` 和按钮完成确认，期望批准/拒绝准确且每次只生效一次）
- [x] **C39：确认期间草稿与界面状态保持。**（验证：打开 Modal 前写入多行草稿，确认或拒绝后检查文本、焦点、滚动和提交状态，期望草稿不丢且界面可继续操作）
- [x] **C40：取消后工具状态与输入区正确恢复。**（验证：在确认框和长命令期间按 `Ctrl+C`，期望 Modal 关闭、工具/助手记录标为取消、composer 恢复且无 worker error）
- [x] **C41：退出后的静态记录有界且脱敏。**（验证：完成成功、拒绝、错误和截断工具场景后退出，期望普通终端保留工具名与状态摘要，但不含隐藏尾部、traceback、API Key 或环境变量值）

## 自动化与回归

> 实际证据：E1、E2；原有 48 项能力已包含在完整 97 项回归中。

- [x] **C42：工具、安全、协议和编排自动化覆盖完整。**（验证：运行完整 pytest，期望六工具、路径逃逸、原子写入、确认、输出截断、双协议碎片及单工具回合均有通过用例）
- [x] **C43：现有交互能力无回归。**（验证：运行全部既有测试，期望多轮对话、thinking、流式文本、Alt+Enter、鼠标滚动、生成中草稿、取消、错误恢复和退出记录继续通过）
- [x] **C44：静态检查全部通过。**（验证：`uv run ruff check .`、`uv run ruff format --check .`、`uv run python -m compileall -q src tests` 均以状态码 0 结束）
- [x] **C45：测试结束无资源泄漏。**（验证：检查完整测试输出，期望无 pending task、未关闭流、worker error 或残留子进程警告）
- [x] **C46：CLI 行为和资源清理保持。**（验证：运行 CLI 测试及 `uv run mewcode --help`，期望帮助和配置错误行为不变，正常/异常退出时 Provider 恰好关闭一次）

## tmux 端到端

> 实际证据：E3-E10。

- [x] **C47：双协议 TUI 可在独立临时项目启动。**（验证：在 tmux 中分别使用本地 OpenAI 与 Anthropic SSE profile 启动 MewCode，输入真实纯文本请求，期望流式答复正常且工具根目录指向临时项目；记录 tmux 版本、pane 尺寸和 profile）
- [x] **C48：读、找、搜工具真实对话通过。**（验证：在 tmux 输入自然语言请求读取指定文件、按模式找文件并搜索内容，期望观察到工具请求、结果摘要、回灌后的最终答复与磁盘事实一致）
- [x] **C49：写、改工具真实对话通过。**（验证：在 tmux 请求新建文件并唯一替换内容，期望最终答复说明结果；从 pane 外核对文件内容与 SHA-256，并验证零/多匹配不修改文件）
- [x] **C50：tmux 中路径越界被拒绝。**（验证：通过真实对话请求读取项目外路径及越界符号链接，期望 TUI 显示安全错误、最终答复可解释，外部文件不泄露且不改变）
- [x] **C51：tmux 中命令批准与拒绝通过。**（验证：先用 `Y` 或 `Enter` 批准带副作用命令，再用 `N` 或 `Esc` 拒绝另一命令，期望批准命令执行、拒绝命令无副作用，二者均产生最终答复）
- [x] **C52：tmux 中限制与取消通过。**（验证：用确定性 SSE 分别触发同批多工具、最终阶段再次调用和含子进程长命令取消，期望没有被禁止的执行或第三次请求，取消后 PID 消失且 TUI 恢复可用）
- [x] **C53：tmux 双 Provider 核心场景一致。**（验证：在 OpenAI 与 Anthropic profile 各完成至少一个文件工具和一个命令确认回合，期望可观察行为一致，并记录 pane 输出和服务端请求次数）
- [x] **C54：退出后保留工具静态记录。**（验证：从 tmux 正常退出到 Shell 后运行 `capture-pane`，期望仍能看到有界的调用、批准/拒绝、结果和限制状态摘要）

## 功能总结

- [x] **C55：本章总结文档可追溯。**（验证：检查 `docs/features/001-tool-system.md`，期望记录六工具、架构、安全边界、配置、实际自动化/tmux 证据、已知限制和对应提交，且 `000-current-features.md` 未被覆盖；实际证据：E11）

## 完成条件

- [x] **C56：工具系统验收完成。**（验证：C1-C55 均有实际证据；实际结果：56/56 通过，无未通过项）

## Spec 覆盖自检

| Spec 标准 | Checklist 条目 |
|-----------|----------------|
| AC1 | C1-C4 |
| AC2 | C5-C7 |
| AC3 | C9-C10 |
| AC4 | C8、C11-C12 |
| AC5 | C8、C13-C14 |
| AC6 | C18-C20、C38-C39、C51 |
| AC7 | C21-C22、C40、C52 |
| AC8 | C7、C15 |
| AC9 | C16-C17 |
| AC10 | C4、C41 |
| AC11 | C37-C41、C54 |
| AC12 | C23-C24 |
| AC13 | C25-C26 |
| AC14 | C32、C52 |
| AC15 | C30-C31、C48-C51 |
| AC16 | C33、C52 |
| AC17 | C27、C34-C35 |
| AC18 | C22、C35-C36、C40、C52 |
| AC19 | C28、C53 |
| AC20 | C29、C43、C46-C47 |
| AC21 | C42-C46 |
| AC22 | C47-C54 |
| AC23 | C55 |

每条 AC 均至少映射到一个可运行或可观察的检查项；内部文件、类型或函数重命名不会改变行为验收判定。
