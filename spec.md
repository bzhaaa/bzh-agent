# MewCode 结构化系统提示与缓存 Spec

## 背景

MewCode 已具备双 Provider、流式对话、六个工具、Agent Loop 与 Plan Mode，但当前没有真正的全局系统提示。普通任务直接把用户消息发送给模型，Plan/Do 约束也被包装成普通用户消息。

这导致几个问题：

- 身份、行为、安全、工具和输出规则缺少稳定统一的约束。
- 模式指令可能被模型视为用户内容，而不是系统级约束。
- 稳定提示、工具描述、环境信息和对话历史没有明确缓存边界。
- Provider 已解析部分缓存 Token，但没有完整表达缓存创建、读取和命中状态。
- 新增自定义指令、Skill 或长期记忆时缺少确定的插入位置和优先级。

## 目标

- 建立按优先级组装的结构化系统提示，固定包含身份、系统约束、任务模式、动作执行、工具使用、语气风格和文本输出七个模块。
- 通过分层的模型请求明确分离稳定提示、动态系统补充消息、工具定义和对话历史。
- 让 Anthropic 使用显式缓存控制，让 OpenAI 保持可自动缓存的稳定前缀。
- 使用不进入历史的 `<system-reminder>` 注入环境、活动模式和其他动态指令。
- 在全局规则与工具描述中双重强化专用工具优先、编辑前先读等约定。
- 提供自定义指令、已激活 Skill 和长期记忆的程序化插槽，但不实现加载来源。
- 统一采集缓存创建与读取指标，为真实 Anthropic 验证和 OpenAI 尽力验证提供证据。
- 准备典型任务场景，以人工方式比较新旧提示下的行为质量。

## 功能需求

- **F1：固定模块。** 全局提示必须包含七个非空模块，按以下顺序排列：身份、系统约束、任务模式、动作执行、工具使用、语气风格、文本输出。
- **F2：确定性组装。** 模块按优先级从前向后拼接，模块之间使用一个空行分隔；相同输入必须得到字节一致的稳定提示。
- **F3：提示分层。** 每次模型请求必须明确分离稳定系统提示、动态系统补充消息、工具定义和对话历史，动态内容不得混入稳定提示。
- **F4：完整指令顺序。** 模型观察到的指令顺序必须为七个固定模块、环境信息、自定义指令、已激活 Skill、长期记忆；空的可选模块直接跳过。
- **F5：可选插槽。** 调用方可以程序化提供自定义指令、Skill 和长期记忆文本；本章默认均为空，不负责从文件或外部存储加载。
- **F6：系统补充消息。** 环境、活动模式和可选模块通过独立系统级补充消息注入，内容使用 `<system-reminder>` 标签，不保存到会话历史，也不作为用户消息展示或回复。
- **F7：环境信息。** 环境补充内容包含项目根目录、操作系统、Shell、当前日期与时区、Git 分支、Git 干净或有改动状态和当前 Agent 模式；不得包含环境变量、用户名、API Key 或完整改动文件列表。
- **F8：模式注入频率。** 每次用户提交开始新的计数：第 1 次模型请求注入完整模式约束，第 2-5 次注入精简提醒，第 6 次再次注入完整约束，第 7-10 次注入精简提醒。
- **F9：Plan/Do 迁移。** `/plan` 和 `/do` 的模式约束改由系统补充消息承载；用户的真实任务仍作为用户消息发送，模式约束不再伪装成用户输入。既有只读工具边界和计划就绪语义保持不变。
- **F10：关键规则双重强化。** 全局工具模块和相关工具描述必须同时强调优先使用专用工具、搜索优先于 Shell、编辑已有文件前先读取、精确小范围修改及根据工具错误调整。
- **F11：提示层约束。** “编辑前先读”只作为提示与工具描述规则，不新增运行时读取状态或编辑拦截。
- **F12：工具描述稳定性。** 相同模式下的工具名称、顺序、描述和参数结构必须稳定；Normal 与 Plan 可以形成各自独立的稳定工具前缀。
- **F13：Anthropic 缓存。** 稳定系统提示与稳定工具描述使用协议原生显式缓存控制；环境、活动模式、可选模块和历史位于缓存断点之后。
- **F14：OpenAI 缓存。** 请求保持稳定内容在前、动态内容在后的确定顺序，使用服务端自动 Prompt Cache，不发送 Anthropic 专属缓存参数。
- **F15：缓存指标。** 统一用量事件能够表达普通输入、输出、缓存创建和缓存读取 Token。Anthropic 解析创建与读取字段，OpenAI 解析缓存输入 Token；字段缺失时明确为未知，不伪装成零。
- **F16：缓存指标边界。** 缓存指标只通过 Provider/Agent 事件提供，不修改当前 TUI Token 状态或静态 transcript。
- **F17：协议兼容。** 结构化提示不得破坏 Anthropic extended thinking、双 Provider 工具调用、多迭代历史检查点、取消和流错误处理。
- **F18：缓存实测。** 使用真实 Anthropic profile 重复请求相同稳定前缀，必须观察并记录缓存创建和缓存读取；OpenAI 进行同类验证，但兼容服务或模型不返回缓存字段时记录为未知而不阻塞验收。
- **F19：人工对比场景。** 准备一组固定任务，覆盖专用工具选择、编辑前读取、Plan 只读、安全边界、代码风格和输出简洁度，供相同模型在旧提示与新提示下人工比较。

## 非功能需求

- **N1：职责解耦。** 提示模块组装、动态指令调度、环境采集、缓存策略、Provider 协议转换和 Agent 循环保持独立；Provider 不决定提示内容，提示层不解析流式响应。
- **N2：确定性缓存前缀。** 相同版本、相同模式和相同工具集合必须生成完全一致的稳定前缀；日期、Git 状态、项目路径和历史变化不得改变该前缀。
- **N3：动态内容有界。** 环境和可选模块必须有明确长度边界，不得因长记忆、Skill 或自定义文本造成无界请求增长。
- **N4：敏感信息隔离。** 提示、事件、测试输出和验收日志不得包含 API Key、环境变量值、用户主目录身份信息或未脱敏的认证错误。
- **N5：协议隔离。** Agent 和 Session 只能处理供应商无关的提示请求与缓存用量；Anthropic `cache_control`、OpenAI 缓存明细等原生字段只能出现在对应 Provider 内。
- **N6：缺失值诚实传播。** Provider 未返回缓存字段时必须保持未知；不得根据总输入 Token 推算命中量，也不得把不支持解释成零命中。
- **N7：环境容错。** 非 Git 项目、detached HEAD、Git 命令失败或 Shell 信息缺失时仍可发起模型请求，并在环境消息中使用安全的未知状态。
- **N8：配置兼容。** 现有六字段 YAML profile 不新增必填项；已有 OpenAI、Anthropic 和 thinking 配置可以直接启动。
- **N9：行为兼容。** Agent Loop、Plan/Do、六工具、命令确认、多轮历史、取消、错误脱敏和 TUI 交互不得回归。
- **N10：可测试性。** 模块排序、标签、注入频率、稳定前缀、Provider 映射和缓存字段解析都能使用本地假 Provider 与确定性流验证，不依赖付费 API。
- **N11：真实缓存成本受控。** Anthropic 实测只发送满足缓存门槛所需的最少重复请求；OpenAI 尽力验证同样有明确次数上限，不执行开放式重试。
- **N12：定性评估可复现。** 人工对比场景固定任务文本、模型、模式、项目夹具和观察维度；记录实际行为，不把主观结论伪装成自动评分。
- **N13：验收真实性。** 开发完成后必须在 tmux 中启动 MewCode，实际观察结构化提示下的普通任务、Plan/Do、工具选择和最终答复，并对照 `checklist.md` 记录结果。
- **N14：文档同步。** 验收后生成编号 003 的 MewCode 功能总结与 Claude Code 对照文档，并更新功能文档目录。
- **N15：后续可扩展性。** 后续接入项目指令文件、真实 Skill、长期记忆或 MCP 时，只需向现有插槽提供内容，不需要重写 Provider 或历史模型。

## 不做的事

- 不加载 `AGENTS.md`、`CLAUDE.md`、项目级指令文件或用户全局指令文件。
- 不自动发现、激活或执行真实 Skill；本章只提供已激活 Skill 文本插槽。
- 不实现长期记忆的创建、检索、持久化、压缩或自动注入。
- 不接入 MCP、插件、远程工具或动态工具注册。
- 不实现自动化提示质量评分、LLM-as-judge、基准排行榜或统计显著性分析。
- 不为“编辑前先读”增加运行时状态机、文件版本校验或硬性拦截。
- 不新增通用权限系统、工具 allow/ask/deny 规则或文件编辑确认。
- 不把缓存指标添加到 TUI、静态 transcript 或用户对话内容。
- 不新增 YAML 提示模板、缓存开关、注入间隔或可选模块配置字段。
- 不允许用户通过对话修改七个固定模块的顺序或优先级。
- 不把环境信息、模式提醒或可选模块写入持久会话历史。
- 不缓存动态环境、当前模式提醒、对话历史或工具执行结果。
- 不实现主动缓存预热、缓存保活、缓存失效重试或为追求命中而发送额外开放式请求。
- 不保证所有 OpenAI 兼容服务都提供自动缓存或缓存明细；缺失时只报告未知。
- 不切换到 OpenAI Responses API，继续使用现有 Chat Completions。
- 不改变 Anthropic extended thinking 的预算和展示行为。
- 不实现上下文压缩、历史摘要、Token 成本预算或会话持久化。
- 不修改现有六个工具的执行结果、目录边界、并发策略或命令确认契约。
- 不自动根据人工对比结果改写提示；评估结果只作为后续调整依据。

## 验收标准

- **AC1（F1、F2）：** 给定固定输入组装全局提示，连续运行得到字节一致的结果；七个模块全部非空，顺序严格为身份、系统约束、任务模式、动作执行、工具使用、语气风格、文本输出，模块间恰有一个空行。
- **AC2（F3、F4）：** 捕获一次模型请求，可以分别观察稳定系统提示、系统补充消息、工具定义和历史；完整指令顺序为七个固定模块、环境、自定义指令、Skill、长期记忆。
- **AC3（F5）：** 分别提供三个可选模块及空值，非空内容按固定顺序出现，空模块不产生标题、标签或多余空行。
- **AC4（F6）：** 系统补充内容使用 `<system-reminder>` 标签，并通过两种 Provider 的系统级协议位置发送；请求结束后会话历史和 TUI transcript 均不包含该消息。
- **AC5（F7、N4、N7）：** 在 Git 项目、非 Git 项目和 detached HEAD 环境中生成补充消息，均能安全完成；内容包含规定字段，但不包含 API Key、环境变量值、用户名或完整改动文件列表。
- **AC6（F8）：** 对一次达到 10 次模型请求的 Agent Run，捕获到第 1、6 次为完整模式提醒，第 2-5、7-10 次为精简提醒；新用户提交后计数从完整提醒重新开始。
- **AC7（F9）：** `/plan <任务>` 的任务正文保持用户消息，Plan 约束位于系统补充消息；`/do` 后切换为 Normal 系统约束。Plan 仍只暴露三个读工具，无成功计划时仍不请求模型。
- **AC8（F10、F11）：** 检查稳定全局提示和六个工具描述，能够同时找到专用工具优先、搜索优先、编辑前读取和精确修改规则；直接调用编辑工具仍沿用原执行契约，不因缺少读取记录被程序拒绝。
- **AC9（F12）：** 相同模式连续生成工具定义时名称、顺序、描述和参数结构完全一致；Plan 与 Normal 分别形成稳定的三工具和六工具集合。
- **AC10（F13）：** 捕获 Anthropic 请求，稳定提示和工具定义带有合法缓存控制，环境、模式、可选模块及历史位于缓存边界之后，动态变化不改变稳定部分。
- **AC11（F14）：** 捕获 OpenAI 请求，稳定提示始终位于动态补充和历史之前，不包含 Anthropic 专属字段；改变日期、Git 状态或用户消息后，稳定前缀仍保持一致。
- **AC12（F15、N6）：** 分别输入 Anthropic 缓存创建/读取字段和 OpenAI 缓存输入字段，统一用量事件返回对应数值；字段缺失或兼容服务不支持时返回未知而不是零或估算值。
- **AC13（F16）：** 产生缓存用量事件后，现有 TUI 状态和退出 transcript 不新增缓存文本，事件消费者仍可独立读取缓存指标。
- **AC14（F17）：** 开启 Anthropic extended thinking 并执行多工具 Agent Loop，thinking、工具调用、历史检查点、取消和最终答复保持正常；OpenAI 执行相同领域场景也能完成。
- **AC15（F18、N11）：** 使用真实 Anthropic profile 发送有界的重复请求，实际观察到首次缓存创建和后续缓存读取字段，并记录模型、请求次数和脱敏结果。
- **AC16（F18）：** 使用真实 OpenAI profile 进行有界重复请求；若返回缓存明细则记录实际命中量，否则明确记录为未知或当前服务不支持，且不判定整体验收失败。
- **AC17（F19、N12）：** 使用固定模型、项目夹具和任务文本完成人工前后对比，逐项记录专用工具、先读后改、Plan 只读、安全边界、代码风格和输出简洁度的实际表现，不生成自动分数。
- **AC18（N3）：** 输入超出规定边界的可选模块时产生明确、有界的拒绝结果；正常长任务中动态提示不会随 Agent 迭代重复累积进历史。
- **AC19（N8、N9、N10）：** 原六字段 YAML 可以直接启动；完整自动化测试覆盖模块组装、注入频率、协议映射、缓存字段和既有 Agent 行为，全部质量检查通过。
- **AC20（N13）：** 在 tmux 中启动真实 MewCode，输入普通编码任务和 Plan/Do 请求，观察系统提示驱动正确的工具选择、模式边界、流式答复和多轮继续能力，并把实际结果写入验收记录。
- **AC21（N14）：** 新增编号 003 的 MewCode 功能总结、Claude Code 对照文档和人工场景文档，目录链接、核对日期、实现提交及自动化/tmux/真实缓存证据均可追溯。
- **AC22（N15）：** 使用仅程序化提供的自定义指令、Skill 和记忆插槽完成请求，不需要修改 Provider、Agent 历史结构或七个固定模块的排序逻辑。
