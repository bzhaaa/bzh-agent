# MewCode 工具系统 Spec

## 背景

MewCode 当前是支持 OpenAI 与 Anthropic、流式输出和多轮上下文的终端对话助手，但模型只能生成文本，不能实际查看或修改项目，也不能运行验证命令。

本阶段为 MewCode 增加第一版工具系统。模型可以根据用户请求选择一个工具，MewCode 执行该工具，将结构化结果回灌给模型，再请求模型生成本回合的最终文字答复。工具定义、执行和错误采用供应商无关的统一模型；OpenAI Chat Completions 与 Anthropic Messages 分别在 Provider 内完成协议转换。

## 目标

- 建立统一、可注册、可扩展的工具接口与注册中心。
- 提供读文件、写文件、改文件、执行命令、查找文件和搜索代码六个核心工具。
- 所有文件操作严格限制在启动时的项目根目录内。
- 支持两种 Provider 的流式工具调用及 JSON 参数碎片拼接。
- 将工具成功、失败、拒绝和超时结果以结构化形式回灌给模型。
- 每个用户回合最多执行一个工具，并在结果回灌后生成一次最终答复。
- 命令执行前通过 TUI 模态框获得用户明确批准。
- 为下一阶段的自动 Agent Loop 保留稳定的领域模型和协议边界。

## 功能需求

- **F1：统一工具接口。** 每个工具提供唯一名称、用途描述、JSON 参数 Schema 和异步执行能力，并返回统一的结构化结果。
- **F2：工具注册中心。** 系统集中登记工具，拒绝重复名称，支持按名称查找，并能转换为 OpenAI 与 Anthropic 接受的工具定义。
- **F3：项目根目录。** MewCode 启动时固定当前工作目录为项目根目录；所有文件类工具只能访问该目录及其子目录。
- **F4：路径安全。** 文件工具拒绝目录穿越、项目外绝对路径和解析后越过项目根目录的符号链接；错误中返回安全、清晰的原因。
- **F5：读文件。** 模型可读取项目内 UTF-8 文本文件，并可指定起始行和读取行数；目录、二进制文件、无效范围和不存在路径返回结构化错误。
- **F6：写文件。** 模型可在项目内创建文件或完整覆盖已有文件；自动创建缺失的父目录，结果明确标注“新建”或“覆盖”。
- **F7：改文件。** 模型提交目标文件、唯一原文和替换文本；原文恰好匹配一次时执行替换，匹配零次或多次时不修改文件并返回明确错误。
- **F8：执行命令。** 模型提交命令和可选超时；MewCode 在项目根目录执行，并捕获退出码、标准输出、标准错误、超时和启动失败。
- **F9：命令确认。** 每次执行命令前显示 TUI 模态框，完整展示命令、工作目录和超时；用户批准后执行，拒绝后不启动进程并生成结构化拒绝结果。
- **F10：按模式找文件。** 模型可使用 glob 模式查找项目内文件，返回按相对路径排序的结果；不跟随越界符号链接，并对结果数量设置上限。
- **F11：搜索代码内容。** 模型可用文本或正则模式搜索项目内文件内容，可附加 glob 过滤；返回相对路径、行号和匹配行，并对结果数量与单行长度设置上限。
- **F12：统一执行器。** 所有工具调用都经过同一执行入口，统一完成按名查找、参数 Schema 校验、超时控制、异常捕获和结果序列化。
- **F13：结构化结果。** 工具结果至少包含工具名、调用标识、成功状态、结果内容或错误代码与错误说明；可恢复失败作为普通工具结果回灌，不使应用崩溃。
- **F14：工具结果展示。** TUI 按对话顺序展示工具请求、确认状态和执行结果摘要；退出后的静态对话记录保留这些状态，但不输出被截断的隐藏内容或敏感信息。
- **F15：OpenAI 流式工具调用。** Provider 能解析 Chat Completions 流中的工具调用标识、名称和 JSON 参数碎片，完成后转换为统一工具调用事件。
- **F16：Anthropic 流式工具调用。** Provider 能解析 Messages 流中的工具使用内容块和 JSON 参数碎片，完成后转换为同一套工具调用事件。
- **F17：单工具限制。** 每个用户回合最多接受一个工具调用；模型同批请求多个工具时全部不执行，并将结构化限制错误回传给模型。
- **F18：单次工具回合。** 首次模型响应请求一个工具时，MewCode 执行或拒绝该工具，将助手工具请求与工具结果加入本轮上下文，然后只再请求模型一次生成最终文字答复。
- **F19：禁止连续调用。** 结果回灌后的第二次模型响应若再次请求工具，该调用不执行；本回合以明确的限制状态结束，不启动第三次模型请求。
- **F20：历史一致性。** 完整工具回合成功结束后，后续对话保留用户消息、助手工具请求、工具结果和最终答复；取消、协议损坏或未完成的流不得留下半轮上下文。
- **F21：取消。** 生成或工具执行期间按 `Ctrl+C` 可取消当前工作；已完成的文件写入不会自动回滚，正在运行的命令进程必须终止，未完成轮次不进入后续上下文。
- **F22：双 Provider 一致性。** 除供应商原生消息格式外，两种 Provider 对工具定义、调用、结果、限制错误和最终答复表现一致。

## 非功能需求

- **N1：安全边界。** 文件系统边界以启动时解析后的项目根目录为准，每次操作都在执行前重新解析目标路径，不能依赖未经校验的字符串前缀。
- **N2：命令知情同意。** 未经用户针对本次完整命令明确批准，不得启动任何子进程；确认不得复用于下一次调用。
- **N3：结果有界。** 文件读取、搜索结果、命令输出和错误文本均设置明确上限；发生截断时结果必须标注，避免上下文和 UI 无界增长。
- **N4：参数可靠性。** 模型生成的 JSON 参数必须经过完整解析和 Schema 校验；缺失字段、未知字段、类型错误和无效 JSON 都返回可恢复的结构化错误。
- **N5：原子文件操作。** 写文件和改文件应使用同目录临时文件替换目标，避免进程中断留下部分内容；改文件匹配失败时目标保持逐字节不变。
- **N6：编码明确。** 第一版文件内容统一按 UTF-8 处理；无法解码的文件不返回乱码，也不自动修改。
- **N7：超时与取消。** 所有工具都有默认和最大超时；取消或超时后释放文件、任务和子进程资源，不留下后台进程。
- **N8：错误隔离。** 工具自身异常、参数错误和操作系统错误不能逃逸到 TUI；面向模型的错误可操作，面向用户的显示不含 traceback、API Key 或环境变量值。
- **N9：协议隔离。** OpenAI 与 Anthropic 的工具定义、流式事件和消息格式只存在于对应 Provider；注册中心、执行器和会话层不识别供应商协议。
- **N10：流式完整性。** 只有工具调用标识、名称和参数全部完成且流正常结束后才允许执行；重复、乱序、残缺或冲突的参数片段不得触发工具。
- **N11：会话事务性。** 本轮只有在最终文字答复正常完成后才整体提交历史；失败、取消或连续工具调用限制不得留下无法被后续 Provider 正确重放的半轮记录。
- **N12：界面响应。** 等待模型、命令确认和工具执行期间 TUI 保持可响应；命令确认框必须支持纯键盘操作且不会丢失输入框草稿。
- **N13：可测试性。** 工具、路径安全、执行器、双 Provider 流解析和会话编排均可使用临时目录及本地假流测试，不依赖付费 API。
- **N14：兼容性。** 保留现有纯文本回复、多轮会话、thinking、取消、静态记录和配置选择行为；没有工具调用时不增加额外模型请求。
- **N15：可扩展性。** 新增工具只需实现统一接口并注册；下一阶段增加 Agent Loop 时无需修改六个工具的执行契约。
- **N16：验收真实性。** 开发完成后必须在 tmux 中启动 MewCode，使用真实对话触发文件工具和命令确认，并按 checklist 记录实际结果。

## 不做的事

- 不实现自动 Agent Loop；一个用户回合最多执行一个工具，并最多追加一次最终答复请求。
- 不执行同批多个工具，也不做并行工具调用。
- 不在最终答复阶段执行第二个工具；模型再次请求工具时以限制状态结束。
- 不实现工具调用重试、自动纠错或自动补参；参数错误作为结果交给最终答复处理。
- 不实现命令白名单、Shell 沙箱、容器隔离或操作系统权限降级；命令安全依赖逐次用户确认。
- 不保证用户批准的 Shell 命令无法访问项目外路径、网络或其他系统资源。
- 不为读文件、写文件、改文件、查找文件和搜索内容增加逐次确认。
- 不实现文件修改预览、diff 审批、撤销、自动备份或事务回滚。
- 不支持二进制文件编辑、非 UTF-8 自动探测或编码转换。
- 不支持符号链接越过项目根目录，即使链接本身位于项目内。
- 不实现 Git 自动提交、分支管理、补丁栈或冲突解决。
- 不实现 MCP、远程工具、插件市场或运行时动态加载第三方工具。
- 不实现会话持久化、恢复或跨进程工具历史。
- 不改变现有 YAML Provider 配置格式；工具超时和输出上限第一版使用程序默认值。
- 不支持 OpenAI Responses API；继续使用 Chat Completions 工具调用协议。
- 不将 Claude thinking 内容写回后续上下文。

## 验收标准

- **AC1（F1、F2、F12）：** 六个工具均通过统一接口注册；重复名称被拒绝；注册中心能按名称查询，并分别生成合法的 OpenAI 与 Anthropic 工具定义。
- **AC2（F3、F4、N1）：** 从项目内外、`..`、绝对路径和越界符号链接尝试访问时，只有解析后仍位于项目根目录内的路径可执行，其余返回结构化安全错误。
- **AC3（F5、N3、N6）：** 读文件能按行读取 UTF-8 文本并标注截断；不存在、目录、二进制、非 UTF-8 和无效范围均返回明确错误。
- **AC4（F6、N5）：** 写文件能创建父目录、新建文件和原子覆盖已有文件，结果正确标明“新建”或“覆盖”，中断测试不留下部分文件。
- **AC5（F7、N5）：** 改文件仅在原文恰好匹配一次时原子替换；零次或多次匹配时文件逐字节不变，并返回可供模型修正的错误。
- **AC6（F8、F9、N2）：** 每次命令调用都先显示完整命令、项目工作目录和超时；批准后才启动，拒绝时无子进程启动且模型收到结构化拒绝结果。
- **AC7（F8、N3、N7）：** 命令成功、非零退出、启动失败、超时和取消均返回有界结果；包含退出码、stdout、stderr 和截断状态；超时或取消后不存在残留子进程。
- **AC8（F10）：** glob 文件查找返回项目内、按相对路径排序的文件，结果有数量上限，且不通过符号链接泄露项目外路径。
- **AC9（F11）：** 内容搜索支持文本、正则和 glob 过滤，返回相对路径、行号与匹配行；无匹配、非法正则、结果截断和长行截断均可观测。
- **AC10（F12、F13、N4、N8）：** 未知工具、无效 JSON、Schema 不匹配、超时和工具异常都被转换为统一结果；TUI 不崩溃，不显示 traceback、密钥或环境变量值。
- **AC11（F14、N12）：** TUI 按顺序展示工具请求、命令确认和结果摘要；确认期间界面可操作，批准或拒绝后输入草稿保持；退出后的静态记录包含必要工具状态。
- **AC12（F15、N10）：** OpenAI 流中的调用标识、工具名和多段 JSON 参数能正确拼接；残缺、冲突、多调用或异常结束的流不会执行工具。
- **AC13（F16、N10）：** Anthropic 流中的工具使用块和多段 JSON 参数能转换为同一调用模型；残缺、冲突、多调用或异常结束的流不会执行工具。
- **AC14（F17）：** 模型同批请求两个或更多工具时，所有工具均不执行，模型收到“每回合只允许一个工具”的结构化错误。
- **AC15（F18）：** 单个工具请求会被执行或拒绝，结果以供应商原生格式回灌，然后只发起一次最终答复请求；最终文字答复完成后整轮进入历史。
- **AC16（F19）：** 最终答复请求若再次产生工具调用，该工具不执行，也不发起第三次模型请求；UI 显示本章不支持连续调用的限制状态。
- **AC17（F20、N11）：** 成功工具回合可被两种 Provider 在下一轮完整重放；取消、无效流、工具编排失败和连续调用限制不会留下半轮历史。
- **AC18（F21、N7）：** 生成中或工具执行中按 `Ctrl+C` 能取消本轮；运行命令被终止，已完成的文件变更不回滚，未完成轮次不进入上下文。
- **AC19（F22、N9）：** 使用同一组领域级场景测试 OpenAI 与 Anthropic，除协议载荷外，工具选择、结果、错误、限制和最终答复一致。
- **AC20（N14）：** 模型直接返回文本时仍只请求一次；现有多轮对话、thinking、草稿编辑、滚动、取消、错误恢复和退出记录测试继续通过。
- **AC21（N13）：** 自动化测试覆盖六个工具、路径逃逸、原子写入、参数校验、命令确认、输出截断、双 Provider 参数碎片和单工具回合编排。
- **AC22（N16）：** 在 tmux 中使用真实对话分别触发读文件、写/改文件、搜索和命令工具；实际观察命令批准与拒绝、工具结果回灌和最终答复，并逐项记录 checklist 证据。
- **AC23（功能总结）：** 验收完成后新增 `docs/features/001-tool-system.md`，记录本章行为、架构、配置、测试证据、限制及对应提交，不覆盖 `000-current-features.md`。
