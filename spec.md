# MewCode Agent Loop Spec

## 背景

MewCode 已具备统一工具接口、六个核心工具、双 Provider 工具调用解析和结构化结果回灌，但每个用户回合最多只能执行一个工具。工具结果返回后，模型只能再生成一次最终文字；如果再次请求工具，系统会停止。这使模型无法根据文件内容、命令输出或工具错误持续调整做法，复杂任务仍需要用户反复发送“继续”。

本阶段为 MewCode 增加自主 Agent Loop。模型可以反复执行“分析当前上下文、选择工具、观察结果、调整下一步”，直到不再请求工具或触发明确停止条件。同时引入统一异步事件流、安全的多工具调度，以及 `/plan` 到 `/do` 的只读规划和执行流程。

本阶段保留上一章现有的命令逐次确认，不新增通用权限系统；保留现有工具契约、双 Provider、流式输出、thinking、取消和多轮会话能力。

## 目标

- 建立可连续调用模型与工具的 ReAct 循环，无需用户逐步催促。
- 对正常完成、10 次迭代上限、用户取消、连续两轮只请求未知工具和模型流错误提供确定的停止行为。
- 通过统一异步事件表达文本、thinking、工具调用、工具结果、Token 用量、循环进度和最终停止状态，使 Agent 不依赖 TUI。
- 在实时向界面转发流内容的同时，收集完整模型响应供循环决策和历史记录使用。
- 同批工具按原始顺序分段执行：连续读类工具并发，写文件、改文件和命令工具逐个串行。
- 支持持久两阶段 Plan Mode：`/plan <任务>` 只使用读类工具形成计划，`/do` 切回全工具并执行已形成的计划。
- 按完整迭代保存历史；发生取消或流错误时，保留已经完整结束的工具请求与结果，丢弃未完成的当前响应。
- 为后续权限系统、上下文压缩和更丰富的用户交互保留稳定扩展边界。

## 功能需求

- **F1：自主循环。** 每条普通用户请求启动一次 Agent Loop，系统反复请求模型、执行工具并回灌结果，直到模型返回不含工具调用的完整文字答复。
- **F2：迭代语义。** 每次模型请求计为一次迭代；完整响应含工具调用时执行工具并进入下一次迭代，不含工具调用时结束本轮。
- **F3：迭代上限。** 每次 Agent Loop 最多进行 10 次模型请求；达到上限后不得发起第 11 次请求，并向界面产生明确的上限停止状态。
- **F4：未知工具停止。** 模型某次响应只请求未知工具时，将每个调用转换为结构化错误并回灌；连续两次迭代均只请求未知工具时停止。任一有效工具调用会清零连续计数。
- **F5：用户取消。** 用户可以在模型生成、并发工具组、串行工具或命令确认期间取消；系统停止后续迭代并清理仍在运行的任务。
- **F6：流错误停止。** 模型流缺失正常结束、事件冲突、工具参数残缺或 Provider 报错时立即停止，不执行不完整调用，也不继续请求模型。
- **F7：统一异步事件。** Agent 对外按发生顺序产生 thinking、文本、工具调用、工具结果、Token 用量、进度和停止事件；事件不包含 TUI 类型或界面操作。
- **F8：双路流式收集。** thinking 和文本增量到达时立即向消费者转发；同时收集完整文字、完整工具调用和结束状态，用于判断是否继续循环。
- **F9：响应完整性。** 只有正常结束且工具调用标识、名称和参数完整的模型响应才能进入工具执行；不完整响应不得产生副作用。
- **F10：多工具分段。** 同一模型响应包含多个工具时，按原调用顺序划分执行段：相邻读文件、找文件和搜代码调用组成并发段；写文件、改文件和执行命令分别组成单独串行段。
- **F11：确定性回灌。** 即使并发读工具完成顺序不同，写入模型历史和回灌 Provider 的工具结果仍按原工具调用顺序排列，并与调用标识一一对应。
- **F12：批次错误隔离。** 一个工具返回普通失败时，不取消同一批其他调用；每个调用都获得独立结构化结果。系统级取消或任务异常则停止尚未完成的执行。
- **F13：命令确认兼容。** `execute_command` 在 Agent Loop 中继续沿用上一章的逐次确认；批准、拒绝、超时和取消都作为对应工具结果或停止事件进入循环。
- **F14：进入 Plan Mode。** 用户输入非空的 `/plan <任务>` 后进入持久只读模式，并立即以该任务启动 Agent Loop。
- **F15：Plan Mode 工具范围。** Plan Mode 只向模型提供 `read_file`、`find_files` 和 `search_code`；后续普通消息继续在只读模式中完善计划，直到用户执行 `/do`。
- **F16：计划完成。** Plan Mode 中模型返回不含工具调用的完整文字时，本次规划请求结束，但模式保持为 Plan；最近一次成功完成的规划上下文可供 `/do` 使用。
- **F17：执行计划。** 只有存在成功完成的规划上下文时，`/do` 才切回全工具模式并启动新的 Agent Loop，根据当前会话中的任务、调查结果和计划执行；否则本地提示没有可执行计划，不请求模型。
- **F18：模式可见性。** 模式进入、保持、退出及当前迭代进度通过统一事件提供给界面；TUI 能明确显示当前处于 Plan 或执行模式。
- **F19：Token 用量。** 每次模型请求结束后产生本次请求及当前 Agent Loop 累计的输入、输出和总 Token 用量；Provider 未返回某项用量时明确标记未知，不估算或伪造。
- **F20：历史检查点。** 用户消息、完整模型工具请求和对应全部工具结果构成一个可提交检查点；检查点完成后立即进入会话历史。
- **F21：中途停止历史。** 取消、流错误、未知工具停止或迭代上限发生时，保留此前完整检查点，丢弃当前未完成模型响应；已经产生的文件或命令副作用不自动回滚。
- **F22：文本与工具共存。** 模型在工具调用前产生的普通文本实时展示，并与该次完整工具请求一起保存；它不被误判为最终答复。
- **F23：纯文本快速路径。** 模型第一次响应直接给出完整文字时，只发起一次请求并正常结束，不因 Agent Loop 增加额外调用。
- **F24：双 Provider 一致性。** OpenAI 与 Anthropic 除原生协议事件和可用 Token 字段外，对循环、并发调度、停止条件、Plan Mode、历史和界面事件表现一致。

## 非功能需求

- **N1：职责解耦。** Agent 循环、流式收集、工具调度、Provider 协议适配和界面展示必须保持独立；Agent 不调用 TUI 组件，TUI 不判断循环是否继续。
- **N2：界面响应。** 模型流式生成、并发读工具、串行副作用工具和命令确认期间，TUI 必须保持可滚动、可编辑和可取消。
- **N3：有界执行。** 单次循环受 10 次模型请求上限约束；每个工具继续使用既有超时和输出上限，事件与内部收集不得导致无界文本复制。
- **N4：取消可靠性。** 取消必须传播到当前模型流和全部尚未完成的工具任务；正在运行的命令继续沿用进程组终止与资源回收保证，不遗留后台任务。
- **N5：并发安全。** 只有无副作用的读类工具可以并发；写文件、改文件和命令始终按模型给出的相对顺序串行执行，不允许两个副作用工具重叠运行。
- **N6：结果确定性。** 并发完成时序不得改变回灌历史的调用顺序、调用标识对应关系或双 Provider 的后续消息结构。
- **N7：协议隔离。** Agent Loop 和工具调度不得识别 OpenAI 或 Anthropic 原生事件；Token 用量、工具调用和完成状态由 Provider 转换为统一模型。
- **N8：历史合法性。** 任意已提交检查点都必须能被两种 Provider 独立重放；不得提交缺少工具结果、工具标识不匹配或未正常结束的模型响应。
- **N9：安全延续。** 文件路径边界、原子写入、结果限长和命令逐次确认保持有效；增加循环和并发不得扩大现有工具的访问范围。
- **N10：配置兼容。** 本章不修改现有 YAML profile 字段；10 次迭代上限使用程序默认值，但在测试和内部组装时允许替换。
- **N11：行为兼容。** 现有纯文本对话、thinking 展示、多轮历史、草稿编辑、鼠标滚动、取消、错误脱敏、静态 transcript 和六个工具的独立行为不得回归。
- **N12：可测试性。** Agent 循环、流收集器、停止条件、并发分段、历史检查点、Plan Mode 和 Token 事件均可通过本地假 Provider、可控异步工具和临时目录验证，不依赖付费 API。
- **N13：跨协议一致性。** 同一组领域级测试应可复用于 OpenAI 与 Anthropic；协议测试另外覆盖各自 Token 用量和多工具流解析。
- **N14：验收真实性。** 开发完成后必须在 tmux 中启动 MewCode，使用真实自然语言任务观察多步工具调用、并发读、计划生成、`/do` 执行、停止条件和最终答复，并将实际结果写入 `checklist.md`。
- **N15：文档同步。** 验收完成后新增本章 MewCode 功能总结和 Claude Code 对应实现对照，并更新 `docs/features/README.md` 的同编号配对目录。
- **N16：后续可扩展性。** 后续增加权限策略、上下文压缩或新的界面时，不需要改写 Provider 工具协议或六个核心工具的执行契约。

## 不做的事

- 不实现通用权限系统、工具 allow/ask/deny 规则或权限模式切换。
- 不移除或扩展上一章已有的命令逐次确认；本章只复用现有行为。
- 不实现上下文窗口计算、自动压缩、摘要、记忆文件或 Token 成本预算；Token 用量仅观测和上报。
- 不实现 Provider 请求自动重试、限流退避或故障切换；流错误按停止条件处理。
- 不自动修正未知工具名、补全无效参数或改写模型工具调用；错误结果交给模型自行调整。
- 不实现文件 checkpoint、撤销、自动备份、跨工具事务或副作用回滚。
- 不并发执行写文件、改文件或命令，也不根据模型声明把副作用工具提升为可并发。
- 不分析同批工具之间的数据依赖；只按原始顺序和工具安全分类进行分段。
- 不实现后台 Agent、后台命令、subagent、Agent Team 或跨会话任务调度。
- 不实现 MCP、远程工具、插件或运行时动态注册第三方工具。
- 不实现会话持久化、恢复、fork 或跨进程继续；Plan Mode 状态只存在于当前进程会话。
- 不将计划保存为独立文件，也不要求固定 Markdown 计划格式；计划以正常模型答复保存在当前对话历史中。
- 不增加计划审批弹窗；用户通过继续讨论修改计划，并以输入 `/do` 表示开始执行。
- 不支持 `/do` 指定任意历史计划、计划编号或多个候选计划；只使用当前 Plan Mode 中最近成功完成的规划上下文。
- 不允许 `/plan` 模式调用写文件、改文件或执行命令，即使用户在计划任务文本中要求执行。
- 不增加待发送消息队列；Agent 运行期间仍允许编辑草稿，但普通 `Enter` 不提交新请求。
- 不改变现有 YAML Provider 配置格式，也不增加用户可配置的迭代次数。
- 不支持 OpenAI Responses API；继续使用现有 OpenAI Chat Completions 与 Anthropic Messages 协议。
- 不把 thinking 内容写入后续模型历史；thinking 继续只作为实时界面事件。
- 不依赖模型输出特定的 “Thought/Action/Observation” 文本标签；ReAct 由原生工具调用、工具结果和循环控制实现。

## 验收标准

- **AC1（F1、F2）：** 使用假 Provider 连续返回“读文件 → 搜索内容 → 改文件 → 最终文字”，MewCode 自动完成全部步骤，只需用户发送一次任务。
- **AC2（F3）：** 前 10 次模型响应均请求工具时，第 10 次工具结果正常执行并形成检查点，但系统不发起第 11 次请求，界面收到明确的迭代上限停止事件。
- **AC3（F4）：** 第一次只请求未知工具时，模型收到结构化错误并可继续；连续第二次仍只请求未知工具时停止。中间出现有效工具调用后计数重新开始。
- **AC4（F5、N4）：** 分别在模型生成、并发读工具、串行文件工具、命令确认和命令运行期间取消，后续迭代均不再启动，未完成异步任务和命令进程被回收。
- **AC5（F6、F9）：** 缺少结束事件、冲突事件、残缺工具调用、无效参数碎片和 Provider 异常均停止循环，不执行对应工具，也不提交不完整响应。
- **AC6（F7、F8）：** 消费 Agent 事件时可以实时看到文本增量；流结束后，内部同时获得完整文本、完整工具调用和结束状态，且事件模型不依赖 TUI。
- **AC7（F10、F11、N5、N6）：** 对“读 A、读 B、写 C、读 D、搜 E、改 F”的同批调用，A/B 同时运行，C 单独运行，D/E 同时运行，F 单独运行；各段顺序不变，结果按原调用顺序回灌。
- **AC8（F12）：** 并发读组中一个工具失败时，其余工具仍完成，每个调用都有独立结果；系统取消时所有未完成任务停止。
- **AC9（F13）：** Agent Loop 每次调用命令都显示现有确认框；批准后执行，拒绝后不启动进程，模型收到对应结构化结果并可继续循环。
- **AC10（F14、F15）：** 输入 `/plan <任务>` 后立即进入 Plan Mode；模型只能看到三个读类工具，经过多轮读取和搜索形成计划，普通后续消息仍不能触发写文件、改文件或命令。
- **AC11（F16、F17）：** 计划正常完成后模式仍显示为 Plan；输入 `/do` 后切回全工具并根据当前规划上下文执行。没有成功计划时输入 `/do` 只显示本地提示，不请求 Provider。
- **AC12（F18）：** 界面能够从事件中观察模式切换、当前迭代序号、工具执行阶段和最终停止原因，不读取 Agent 内部状态。
- **AC13（F19）：** 每次模型请求后产生本次及累计 Token 用量事件；OpenAI 与 Anthropic 的已知字段正确累加，缺失字段明确显示未知而不是零或估算值。
- **AC14（F20、F21）：** 完成两个工具迭代后在第三次模型流中取消或制造错误，后续历史保留用户消息及前两个完整工具检查点，不包含第三次部分文本；下一条消息可继续使用这些上下文。
- **AC15（F22）：** 模型先输出说明文字再请求工具时，文字实时显示并与工具请求一起保存，系统继续执行工具，不提前产生最终完成状态。
- **AC16（F23）：** 模型第一次响应直接返回文字时只产生一次 Provider 请求，并正常提交用户消息和最终答复。
- **AC17（F24、N7、N8、N13）：** 使用同一组循环场景验证 OpenAI 与 Anthropic，两者均能重放多工具、多迭代和中途停止后的合法历史，领域层测试不包含供应商原生结构。
- **AC18（N2）：** Agent 连续工作期间，历史区可滚动、输入框可编辑、取消快捷键有效；事件更新不会冻结 TUI 主事件循环。
- **AC19（N9）：** 并发和循环下，目录穿越及越界符号链接仍被拒绝，文件写入保持原子性，工具输出继续受限，命令未经本次批准不启动。
- **AC20（N10）：** 原有 YAML 配置无需新增字段即可启动；默认上限为 10，自动化测试可以使用更小上限快速触发停止。
- **AC21（N11）：** 现有纯文本、thinking、滚动、草稿、取消、静态记录、双 Provider 和六工具测试继续通过。
- **AC22（N12）：** 自动化测试覆盖循环完成、全部停止条件、双路收集、多工具分段并发、错误隔离、历史检查点、Plan Mode、Token 用量和双 Provider 重放。
- **AC23（N14）：** 在 tmux 中用真实自然语言完成至少一个三步以上工具任务，并实际验证 Plan Mode、`/do`、并发读、命令批准与拒绝、取消和迭代上限；逐项记录实际输出。
- **AC24（N15）：** 验收后新增 `docs/features/002-agent-loop.md` 与 `docs/features/002-claude-code-agent-loop.md`，并在功能文档目录中登记配对关系，记录测试和 tmux 证据。
