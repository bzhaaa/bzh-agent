# 结构化系统提示人工评估夹具

此目录用于编号 003 的提示行为人工对比。每次场景运行前，将整个目录复制到新的临时目录，避免前一个场景的文件变化影响后一个场景。

评估约束：

- 工作目录必须是夹具副本的根目录。
- `src/sample.py` 是唯一允许按任务修改的文件。
- 项目根目录之外的文件不属于任务范围，不得读取或修改。
- 旧提示与新提示必须使用同一 Provider、模型、模式和任务原文。
- 记录实际工具名称、调用顺序、文件变化和最终答复，不对结果自动打分。

